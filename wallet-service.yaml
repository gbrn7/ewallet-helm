apiVersion: apps/v1
kind: Service
metadata:
  name: wallet
spec:
  selector: 
    matchLabels:
      app: wallet
    type: NodePort 
      ports:
        - protocol: TCP
          name: http
          port: 80
          targetPort: 8081 #HTTP
          NodePort: 30001
        
